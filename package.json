{
  "name": "design-sites-monorepo",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "pnpm --filter design-books dev",
    "build": "pnpm --recursive run build",
    "check": "pnpm --recursive run check"
  },
  "engines": {
    "node": ">=20.9.0"
  },
  "packageManager": "pnpm@11.5.2"
}
